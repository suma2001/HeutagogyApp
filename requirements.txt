pyrebase
firebase-admin
"""
<div class="dropdown">
    <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">Lessons
        <!-- <span class="caret"></span> -->
    </button>
                                                                
    <ul class="dropdown-menu">
        {% for lname in les %}
            <li><a href="{% url 'home:platform' course lname %}">{{lname}}</a></li>
        {% endfor %}
    </ul>
</div>


<div class="wrapper">
    <div class="container">
             <div class="table-responsive mt-30">
                <table class="table ucp-table table-condensed" id="content-table" style="border-collapse:collapse;">
                    <thead class="thead-s">
                        <tr>
                            <th class="text-center" scope="col">S.No</th>
                            <th class="cell-ta">Course Title</th>
                            <th class="cell-ta">List of Lessons</th>
                        </tr>
                        
                    </thead>
                    <tbody>
                        {% for course, lesson in courses.items %}
                        <tr data-toggle="collapse" data-target="#demo5" class="accordion-toggle">
                            {% for l, les in lesson.items %}
                            <td class="text-center">{{course}}</td>
                            <td class="cell-ta">C{{course}}</td>
                            
                            <td>
                                <div class="dropdown">
                                    <button class="btn btn-primary dropdown-toggle" type="button" data-toggle="dropdown">Lessons
                                    <!-- <span class="caret"></span> -->
                                    </button>
                                    
                                    <ul class="dropdown-menu">
                                    {% for lname in les %}
                                        <li><a href="{% url 'home:platform' course lname %}">{{lname}}</a></li>
                                    {% endfor %}
                                    </ul>
                                    
                                    
                                </div>
                            </td>
                            {% endfor %}
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div> 
    </div>
</div>
"""